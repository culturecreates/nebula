<div class="container">
  <p><%= "Welcome back #{session[:name]}!" if session[:name] %></p>

  <% if I18n.locale == :fr %>
    <%= render partial: "home-fr" %>
  <% else %>
    <%= render partial: "home-en" %>
  <% end %>
</div>
