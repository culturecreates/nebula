<!-- Input: annotations (not used but kept for compatibility), subject, predicate, object, graph -->
<div class="annotation">

  <% # Check if this triple has annotations by looking for the HasAnnotations marker
     annotation_subject = RDF.Statement(subject, predicate, object)
     has_annotations_marker = graph.query([
       annotation_subject,
       RDF::URI("http://www.w3.org/1999/02/22-rdf-syntax-ns#type"),
       RDF::URI("http://kg.artsdata.ca/ontology/HasAnnotations")
     ])
     has_annotations = has_annotations_marker.count > 0
  %>
  
  <% if has_annotations %>
    <% random = rand(10_000) %>
    <div
      class="reference accordion"
      style="--bs-accordion-btn-padding-y: 5px;--bs-accordion-border-width: 0px;"
      id="accordionFlushExample"
    >
      <div class="accordion-item">
        <h6 class="accordion-header">
          <button
            class="accordion-button btn-sm collapsed"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#collapse<%=random%>"
            aria-expanded="false"
            aria-controls="flush-collapseOne"
          >
            <span>Annotations</span>
          </button>
        </h6>
        <div
          id="collapse<%=random%>"
          class="accordion-collapse collapse"
          data-bs-parent="#accordionFlushExample"
        >
          <turbo-frame
            id='annotations-<%= random %>'
            src="<%= entity_triple_annotations_path(
              subject: subject.to_s,
              predicate: predicate.to_s,
              object: object.to_s,
              frame_id: random
            ) %>"
            loading="lazy"
            target="_top"
          >
            <div class="accordion-body">Loading annotations...</div>
          </turbo-frame>
        </div>
      </div>
    </div>
  <% end %>
</div>
