<!-- Input: subject, predicate, object, graph (annotations parameter was removed - now loaded lazily) -->
<div class="annotation">

  <% # Check if this triple has annotations by looking for the annotation count
     annotation_subject = RDF.Statement(subject, predicate, object)
     count_result = graph.query([
       annotation_subject,
       RDF::URI("http://kg.artsdata.ca/ontology/annotationCount"),
       nil
     ])
     annotation_count = count_result.count > 0 ? count_result.first.object.value.to_i : 0
  %>
  
  <% if annotation_count > 0 %>
    <% random = rand(10_000) %>
    <div
      class="reference accordion"
      style="--bs-accordion-btn-padding-y: 5px;--bs-accordion-border-width: 0px;"
      id="accordionFlushExample"
    >
      <div class="accordion-item">
        <h6 class="accordion-header">
          <button
            class="accordion-button btn-sm collapsed"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#collapse<%=random%>"
            aria-expanded="false"
            aria-controls="flush-collapseOne"
          >
            <span><%= annotation_count %> <%= "annotation".pluralize(annotation_count) %></span>
          </button>
        </h6>
        <div
          id="collapse<%=random%>"
          class="accordion-collapse collapse"
          data-bs-parent="#accordionFlushExample"
        >
          <turbo-frame
            id='annotations-<%= random %>'
            src="<%= entity_triple_annotations_path(
              subject: subject.to_s,
              predicate: predicate.to_s,
              object: object.to_s,
              frame_id: random
            ) %>"
            target="_top"
          >
            <div class="accordion-body">Loading annotations...</div>
          </turbo-frame>
        </div>
      </div>
    </div>
  <% end %>
</div>
