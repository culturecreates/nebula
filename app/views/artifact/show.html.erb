<div class="container">
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item"><%= link_to "Artifacts", artifact_index_path %></li>
      <li class="breadcrumb-item active" aria-current="page"><%= @artifact.artifact_id %></li>
    </ol>
  </nav>

  <h1 class="text-truncate">
    <%= @artifact.name ||= @artifact.artifact_id %>
  </h1>
  <%= render partial: "shared/clipboard", locals: { uri: @artifact.uri } %>

  <% if @loaded_artifact_version.present? %>
    <p>
    <%= form_with url: push_latest_artifact_index_path, method: :post, local: true, html: { class: "d-inline", data: { controller: "confirm", action: "submit->confirm#confirm", confirm: "Reload artifact and replace the data in the Artsdata data feed graph?" } } do |form| %>
      <%= form.hidden_field :artifactUri, value: @artifact.uri %>
        <strong>Artifact loaded:</strong>
        <%= link_to @loaded_artifact_version.version.to_s, entity_path(uri: @loaded_artifact_version.dataset_uri.to_s), class: "py-0 px-1" %>
     
     
        <%= form.hidden_field :artifactUri, value: @artifact.uri %>
        <%= form.submit "Reload",
                  class: "btn btn-outline-secondary btn-sm py-0 px-1",
                  data: {
                    disable_with: "Loading...",
                  } %>
        <% end %>
        <%= form_with url: artifact_path, method: :delete, local: true, html: { class: "d-inline", data: { controller: "confirm", action: "submit->confirm#confirm", confirm: "Delete artifact metadata for #{@artifact.artifact_id}? This will also delete the corresponding graph from the Artsdata Knowledge Graph." } } do |form| %>
          <%= form.hidden_field :artifactUri, value: @artifact.uri %>
          <%= form.hidden_field :graph, value: @artifact.graph %>
          <%= form.submit "Delete",
                      class: "btn btn-sm py-0 px-1 btn-outline-danger",
                      data: {
                        disable_with: "Deleting...",
                      } %>
        <% end %>
      </p>
    <% else %>
      <p class="mt-4">Artifact not loaded into Artsdata.</p> 
      <%= form_with url: push_latest_artifact_index_path, method: :post, local: true, html: {  class: "d-inline", data: { controller: "confirm", action: "submit->confirm#confirm", confirm: "Create an Artsdata data feed graph with this artifact?" } } do |form| %>
        <%= form.hidden_field :artifactUri, value: @artifact.uri %>
        <%= form.submit "Load Artifact",
                    class: "btn btn-secondary",
                    data: {
                      disable_with: "Loading...",
                    } %>
      <% end %>
      <%= form_with url: artifact_path, method: :delete, local: true, html: {  class: "d-inline", data: { controller: "confirm", action: "submit->confirm#confirm", confirm: "Delete artifact metadata for #{@artifact.artifact_id}? This will also delete the corresponding graph from the Artsdata Knowledge Graph." } } do |form| %>
        <%= form.hidden_field :artifactUri, value: @artifact.uri %>
        <%= form.hidden_field :graph, value: @artifact.graph %>
        <%= form.submit "Delete",
                    class: "btn btn-danger",
                    data: {
                      disable_with: "Deleting...",
                    } %>
      <% end %>
  <% end %>

  <div class="mt-3">
    <p>
      <%= @artifact.description %>
    </p>
  </div>
  <!-- Toggle Auto-Minting Button -->
  <div class="card mb-4">
    <div class="card-body">
      <h3 class="card-title">Auto-Minting</h3>
      <p>
        Automatically mint events with Artsdata URIs every time this artifact is loaded into an Artsdata data feed graph.
      </p>
      <p>
        Current status:
        <% if @automint_status %>
          <strong>enabled &#10003;
          </strong>
        <% else %>
          <strong>disabled</strong>
        <% end %>
        <%= form_with url: toggle_auto_minting_artifact_index_path, method: :post, local: true, html: { data: { turbo: "false" } }  do |form| %>
          <%= form.hidden_field :graph, value: @artifact.graph %>
          <%= form.hidden_field :new_boolean, value: !@automint_status %>
          <%= form.submit @automint_status ? "Turn Off" : "Turn On",
                      class: "btn btn-secondary",
                      data: {
                        disable_with: "updating...",
                      } %>
        <% end %>
      </p>
    </div>
  </div>

  <div class="card mb-4">
    <div class="card-body">
      <h3 class="card-title">Graph Metadata</h3>
      <p>Metadata for the Artsdata data feed graph used for describing and ranking sources.</p>
      <p>
        <%= link_to "View Data Feed", entity_path(uri: @artifact.graph), class: "btn btn-outline-secondary btn-sm" %>
      </p>
      <%= form_with url: update_graph_metadata_artifact_index_path, method: :post, local: true, html: { data: { turbo: "false" } } do |form| %>
        <%= form.hidden_field :graph, value: @artifact.graph %>

        <div class="mb-3">
          <%= form.label :graph_name, "Name", class: "form-label" %>
          <%= text_field_tag :graph_name, @graph_metadata[:name], class: "form-control"%>
        </div>

        <div class="mb-3">
          <%= form.label :maintainer, "Maintainer", class: "form-label" %>
          <%= url_field_tag :maintainer, @graph_metadata[:maintainer], class: "form-control", placeholder: "website of the organization maintaining the data" %>
        </div>

        <div class="mb-3">
          <%= form.label :rating_value, "Rating Value", class: "form-label" %>
          <%= text_field_tag :rating_value, @graph_metadata[:rating_value], class: "form-control" %>
        </div>

        <div class="mb-3">
          <%= form.label :rating_explanation, "Rating Explanation", class: "form-label" %>
          <%= text_area_tag :rating_explanation, @graph_metadata[:rating_explanation], class: "form-control", rows: 3 %>
        </div>

        <div class="d-flex gap-2">
          <%= form.submit "Save Metadata", class: "btn btn-secondary", data: { disable_with: "Saving..." } %>
          <%= button_tag "Delete Metadata",
            type: "submit",
            name: "delete_metadata",
            value: "true",
            class: "btn btn-danger",
            data: { disable_with: "Deleting..." } %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="card mb-4">
    <div class="card-body">
      <h3 class="card-title">Artifact Versions</h3>
      <p>Versions of artifacts that can be loaded into Artsdata data feed graphs.</p>
      <% if @artifact_versions.present? %>
        <% total_versions = @artifact_versions.count %>
        <% displayed_versions = @artifact_versions.first(100) %>
        <p><strong>Total versions: <%= total_versions %></strong></p>
        <table class="table table-sm">
          <thead>
            <tr>
              <th>#</th>
              <th>Version</th>
              <th>Issued</th>
              <th colspan="2"></th>
            </tr>
          </thead>
          <tbody>
            <% displayed_versions.each_with_index do |version, idx| %>
              <% issued_display = date_display(version.issued.to_s).presence || version.issued %>
              <tr>
                <td><%= idx + 1 %></td>
                <td><%= link_to version.version, entity_path(uri: version.dataset_uri) %></td>
                <td><%= issued_display.presence || "—" %></td>
                <td colspan="2">
                  <div class="d-flex justify-content-end gap-2">
                    <% if version.distribution_uri.present? %>
                      <%= link_to "Files", entity_path(uri: version.distribution_uri.to_s), class: "btn btn-outline-secondary btn-sm" %>
                    <% end %>
                    <%= form_with url: push_version_artifact_index_path, method: :post, local: true, html: { class: "d-inline", data: { controller: "confirm", action: "submit->confirm#confirm", confirm: "Load artifact version #{version.version} into an Artsdata data feed graph?" } } do |form| %>
                      <%= form.hidden_field :artifactUri, value: @artifact.uri %>
                      <%= form.hidden_field :artifactVersionUri, value: version.dataset_uri %>
                      <%= form.submit "Load",
                                  class: "btn btn-secondary btn-sm",
                                  data: {
                                    disable_with: "Loading...",
                                  } %>
                    <% end %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <% if total_versions > 100 %>
          <p><%= link_to "View all #{total_versions} versions", entity_path(uri: @artifact.uri) %></p>
        <% end %>
      <% else %>
        <p class="text-muted">No versions available yet. Check back later.</p>
      <% end %>
    </div>
  </div>
</div>
